# 単位円と三角関数 学習アプリ — ここまでのまとめ

> 更新日: 2026/09/08

## 1. このプロジェクトの目的

**単位円と三角関数(sin / cos / tan)の関係を直感的に表現し、生徒の理解が深まるアプリ**を作ること。

単位円上を動く点 P(cos θ, sin θ) と、各三角関数の「投影・線分」を重ねて表示することで、
次の直感を掴ませることをねらいとしている。

- **sin θ = 高さ**(点Pの y 座標)… 赤で表示
- **cos θ = 横**(点Pの x 座標)… 青で表示
- **tan θ = 傾き**(直線 x=1 との交点の y 座標)… 緑で表示

---

## 2. プロジェクトの現状

| 項目 | 状態 |
|---|---|
| リポジトリ | `c:\Users\manta\Documents\SinCosTan`(GitHub: mantaroutanaka/SinCosTan, main ブランチ) |
| `index.html` | **実装完了**(483行・単一ファイル・外部依存ゼロ) |
| 検証 | **完了** — Node.js でスクリプトを抽出し DOM モック上で実行する自動テスト **55件すべて合格** |

---

## 3. 承認済みプラン(設計)

### 3.1 技術選定

| 項目 | 判断 | 理由 |
|---|---|---|
| レンダリング | **SVG** | 円・線・ラベルが鮮明、拡大に強い、CSSで装飾しやすい |
| バックエンド | なし(純クライアント) | 学校環境でもオフラインで動作、配布が簡単 |
| 言語 | 素の JavaScript | 外部依存ゼロで簡潔に保つ |
| ファイル | `index.html` 1本に集約 | 既存構成と一致、配布容易 |

### 3.2 画面レイアウト

```
┌────────────────────────────────────────────┐
│ ヘッダー: 単位円と三角関数                  │
├───────────────────┬────────────────────────┤
│                   │ 現在の角度 θ (度/ラジアン)│
│   単位円 SVG      │  sin θ = 0.707 (赤)      │
│   (中心: 大きめ)   │  cos θ = 0.707 (青)      │
│                   │  tan θ = 1.000 (緑)      │
│                   │  象限 / 点Pの座標         │
│                   │  象限×符号の対応表        │
├───────────────────┴────────────────────────┤
│ 角度スライダー / ▶再生 / 速度 / 代表角ボタン │
│ ☑ sin/cos のグラフ表示                     │
│ 下部: sin・cos のグラフ(円運動↔波の対応)    │
└────────────────────────────────────────────┘
```

### 3.3 可視化要素(SVG 描画)

1. **単位円**: 半径1の円 + x/y軸 + 目盛り(-1, -0.5, 0.5, 1)
2. **点P と半径線**: 角度θの位置に点 P(cos θ, sin θ)、原点からの線分
3. **cos θ(青)**: P から y 軸への水平投影 → x座標そのもの
4. **sin θ(赤)**: P から x 軸への垂直投影 → y座標そのもの
5. **tan θ(緑)**: 半径線の延長が直線 x=1 と交わる点の y座標(=傾き)。破線で延長を表示
6. **直角三角形のハイライト**: O・(cosθ, 0)・P を結ぶ三角形 + 直角マーク + θ の弧
7. **符号表示(＋/−)**: 象限によって投影の向きが変わる様子を色付きで表示

### 3.4 インタラクション

- **スライダー** で角度を変更(0〜360°、0.5°刻み)
- **円周上の点Pをドラッグ**(ポインタ操作・タッチ対応)でも角度変更
- **▶再生ボタン**: 角度が自動回転するアニメーション(速度: ゆっくり/ふつう/はやい)
- **代表角ボタン**: 0°, 30°, 45°, 60°, 90°, 180°, 270° をワンクリック設定
- **sin/cos グラフ表示**(トグル): 単位円を1周すると波になる対応を可視化
- **常時表示**: 度・ラジアン(π を使った分数表記)両方、数値、象限、符号対応表

### 3.5 主な内部仕様(実装時に引き継ぐ値)

- SVG: `viewBox="0 0 500 500"`、中心 `CX=250, CY=250`、半径 `R=210`
- 色: sin=`#e53935`(赤) / cos=`#1e88e5`(青) / tan=`#43a047`(緑) / 弧=`#ffb74d`(橙)
- 画面 y 軸は下向きのため、点Pは `px = CX + R·cosθ`, `py = CY − R·sinθ` で計算
- tan の点T: `(CX + R, CY − R·tanθ)`、破線は (−1, −tanθ) から (1, tanθ) まで引く
- ラジアン表示: 度数 ×2 を 360 と最大公約数で約分し `π/4` のような分数表記にする
- tan が大きい場合(θ≈90°)は `±∞` と表示
- 象限は 0°/90°/180°/270° を「軸上」、それ以外を第1〜第4象限として判定し対応表をハイライト

---

## 4. 実装・検証の記録(完了)

1. ✅ `index.html` の全面書き換え(HTML+CSS+静的SVG → JS本体 → グラフ描画・操作・初期化)
2. ✅ 検証: Node.js(v24)で `<script>` を抽出し、DOM モック上で実行する自動テストを作成 → **55件すべて合格**
   - 初期化(45°)、スライダー入力、円周ドラッグ(225°)、代表角ボタン、アニメーション再生/停止(dtクランプ込み)、グラフ生成(721点のpolyline)とカーソル位置、象限ハイライト、tan=±∞ 表示、radFrac の約分、0°〜360° の代表角6ケースの sin/cos/tan/象限ラベル
3. ✅ 主要角度(0°, 30°, 45°, 60°, 90°, 180°, 225°, 270°, 315°)で値・象限が仕様どおりであることを確認
4. ✅ ドラッグ・再生・グラフトグルのロジック確認済み(最終的な見た目・操作感はブラウザでの目視確認を推奨)
5. ✅ `git commit` で保存(実装完了コミット)

---

## 5. 前提・留意事項

- ターゲットは高校数学(数学I〜II)の学習者を想定した日本語UI
- 外部ライブラリ・CDN・ネットワーク通信は一切使わない(オフライン完結)
- このMDは作業の引き継ぎ用メモであり、アプリ本体には含めない
